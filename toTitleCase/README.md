# JavaScript Title Case Challenge

This is a simple function to convert strings to Title Case.

### Why this approach?
In this version, I intentionally used a `for...of` loop instead of the `map()` method to practice:
- Iterating through arrays manually.
- Building strings step-by-step.
- Understanding the underlying logic before moving to more advanced ES6 features.
